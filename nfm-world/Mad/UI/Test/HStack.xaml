<TemplatedControl
    xmlns="https://github.com/uwx/worldxaml"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:w="https://github.com/needforrewrite/nfm-world"
    mc:Ignorable="d" d:DataContext="{d:DesignInstance w:HStack}"
    x:Class="NFMWorld.UI.Test.HStack">
    <TemplatedControl.Template>
        <ControlTemplate TargetType="w:HStack">
            <FlexPanel
                FlexDirection="{Binding BoxFlexDirection, RelativeSource={RelativeSourceTemplatedParent}}"
                AlignItems="Center"
                GapColumn="{Binding GapColumn, RelativeSource={RelativeSourceTemplatedParent}}"
                GapRow="{Binding GapRow, RelativeSource={RelativeSourceTemplatedParent}}">
                <ContentPresenter />
            </FlexPanel>
        </ControlTemplate>
    </TemplatedControl.Template>
</TemplatedControl>